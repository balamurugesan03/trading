import AdminDashboardPage from './AdminDashboardPage';

// Reports view reuses the same platform overview shown on the dashboard.
export default function ReportsPage() {
  return <AdminDashboardPage />;
}
